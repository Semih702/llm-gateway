{{- if .Values.proxy.openaiApiKey }}
apiVersion: v1
kind: Secret
metadata:
  name: llm-gateway-openai
  namespace: {{ include "llm-gateway.namespace" . }}
type: Opaque
stringData:
  {{ .Values.proxy.existingSecretKey }}: {{ .Values.proxy.openaiApiKey | quote }}
{{- end }}
